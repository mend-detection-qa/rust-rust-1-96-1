# Pin the exact Rust / Cargo toolchain version this probe targets.
# Rust 1.96.1 bundles Cargo 1.96.1, libssh2 (patched), and the
# timeout/retry fixes that are the subject of this probe.
[toolchain]
channel = "1.96.1"
